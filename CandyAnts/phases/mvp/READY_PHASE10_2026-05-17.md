# Phase 10 진입 준비 노트 (2026-05-17)

> 다음 세션에서 Phase 10 (ui-atoms-foundation) 진입 시 시작점.
> 본 세션(2026-05-16 ~ 17)의 phase-9 후속 sweep + art polishing이 모두 커밋된 후 작성.

---

## 1. 본 세션 산출 (커밋 6건)

| # | SHA | 메시지 |
|---|---|---|
| A | feat(sprite) | normalize ant canvas to 400x431 content-height 410 |
| B | feat(art) | codex painterly background + cookie tile layers + ant hole spawn |
| C | feat(stage) | thin 12px collision tiles + shorten S1/S2 to ~1010px |
| D | feat(skill) | icon + radio toggle + custom mouse cursor + bottom-center toolbar |
| E | feat(i18n) | localize HUD/result overlay/input hint to Korean |
| F | docs | handoff + phase09 art polishing + phase10 ready note |

세부 내역은 `phases/mvp/ART_POLISHING_LOG_2026-05-16.md` (사용자 13 라운드 art polishing 로그)와 본 문서 §4 참조.

---

## 2. 회귀 게이트 (모두 PASS, set_deferred fix 이후 콘솔 클린)

```bash
python scripts/run_test.py tests/Stage02HeadlessTest.tscn   # Phase3Test PASS
python scripts/run_test.py tests/Stage03HeadlessTest.tscn   # Phase4Test PASS (set_monitoring warning 사라짐)
python scripts/run_test.py tests/BlockerOverlapTest.tscn    # §B-1~B-8 PASS
```

추가:
```bash
python scripts/tools/normalize_sprite_canvas.py --check     # 39 ant PNG height 431 PASS
```

---

## 3. Phase 10 진입 명령

```bash
cd D:/claude/godot/CandyAnts
python scripts/execute.py mvp validate    # 세션 시작 1회
python scripts/execute.py mvp              # 상태 확인 — Phase 10 pending
python scripts/execute.py mvp next         # Phase 10 frontmatter + SoT 출력
```

기대 출력:
- SoT: `docs/UI_GUIDE.md`
- SoT-aux: `docs/INPUT_PLAN.md`, `docs/design_handoff/README.md`

---

## 4. Phase 10 작업 범위 (`phases/mvp/phase10-ui-atoms-foundation.md`)

### 4.1 신규 atom 4종 (Custom Control)
- `CButton` — `ButtonKind { PRIMARY, SECONDARY, GHOST }`, pressed 시 `Motion.boop()` 자동
- `Chip` — pill 정보 태그, 5 tint (peach/grape/mint/berry/lemon)
- `Counter` — 110×84, `set_value(n)` 시 `Motion.caPop(big_number)` 자동
- `SkillSlot` — 88×88, 8 state matrix (armed/selected/hover/pressed/empty/disabled)

### 4.2 Motion 헬퍼 freeze
- `scripts/ui/Motion.gd` 시그니처는 Phase 9에서 freeze됨. 추가 변경 시 phase 9 sweep으로 처리.

### 4.3 비-변경 (중요)
- `scenes/ui/HUD.tscn`, `SkillToolbar.tscn` — Phase 10에서는 atom 인스턴스화 안 함 (Phase 11)
- 기존 `scripts/ui/HUD.gd`, `SkillToolbar.gd` — Phase 10 비변경
  - **주의**: 본 세션에서 SkillToolbar.gd를 대폭 수정(icon/toggle/cursor/bottom-center)했으므로, Phase 11 atom 교체 시 본 변경분을 어떻게 atom과 통합할지 plan 단계에서 결정 필요

### 4.4 신규 헤드리스 테스트
- `tests/AtomShowcaseHeadless.gd` — atom instantiate + tween 생성 검증 (1초 이내)
- `tests/MotionPauseSafeTest.tscn` — Phase 9에서 이미 작성됨, 회귀 유지

---

## 5. Phase 10 진입 전 처리할 잔여 (선택)

### 5.1 Phase 9 sweep deferred (`PLAYTEST_HANDOFF_2026-05-16.md` §4.1)
- R5-L1: svg_color_map.json metadata wording cleanup
- R1-L2: Tokens.gd ↔ svg_color_map.json ↔ UI_GUIDE 토큰 SoT consistency 도구
- R6-M2 잔재: UI_GUIDE.md phase 번호 shift 일부 (§3.5/§3.6/§5)

본 항목들은 MEDIUM/LOW이며 `phase09-deferred.md`로 옮겨 phase 10과 병행 가능. 또는 phase 10 plan 시작 시 별도 sweep commit.

### 5.2 본 세션 누적 art polishing의 plan 반영
- `docs/UI_GUIDE.md` §0.5 운영 모델(1인 + AI 자산)은 Phase 9에서 신설됨
- 본 세션에서 추가된 cookie tile layer 분리, ant_hole spawn entrance, painterly background 등은 phase 10 plan에서 UI_GUIDE에 추가 반영할지 결정 (현 시점에는 art-only이므로 UI_GUIDE 미반영 가능)

---

## 6. 알려진 이슈 / 후속 모니터링

### 6.1 해결됨
- ✅ Candy.gd:47 `monitoring = false` Godot in/out signal 경고 → `set_deferred` 전환 (commit B에 포함)
- ✅ Ant 스킬 애니메이션 스케일 불일치 → canvas 정규화 (commit A)
- ✅ Candy 떠있음 → position.y/CollisionShape 보정 (commit B)
- ✅ Blocker 방향감 → flip() (commit D)
- ✅ Skill UI 아이콘/토글/커서 없음 → 전면 개편 (commit D)
- ✅ Stage 1/2 너무 길음 → ~1010px 단축 (commit C)
- ✅ UI 영어 → 한국어 (commit E)

### 6.2 Phase 10에서 검토
- Cookie tile surface/background 2-layer 구조가 phase 10 atoms와 어떻게 합쳐질지 (atom은 UI, terrain은 world — 직접 충돌 없음 예상)
- `Input.set_custom_mouse_cursor` 호출이 phase 10 atom의 hover state와 간섭하지 않는지 (atom showcase 단독 검증에선 영향 없을 듯)

---

## 7. SoT 진입점 (다음 세션 자동 read)

CLAUDE.md `/harness` v3 명세에 따라 세션 시작 시 자동 로드:
- `docs/PRD.md`
- `docs/ARCHITECTURE.md`
- `docs/ADR.md`

자동 로드 제외, phase 10 frontmatter `sot`/`sot_aux`로 명시:
- `docs/UI_GUIDE.md` (1차 SoT)
- `docs/INPUT_PLAN.md`
- `docs/design_handoff/README.md`

본 문서 + `ART_POLISHING_LOG_2026-05-16.md` + `PLAYTEST_HANDOFF_2026-05-16.md`는 reference only.

---

## 8. Notion phase DB

| Phase | 상태 |
|---|---|
| 9: ui-theme-assets | 완료 (1b9bc69 push 시 동기화됨) |
| 10: ui-atoms-foundation | 시작 전 (Phase 10 진입 시 `진행 중`으로 갱신) |

`phases/mvp/notion-phase-ids.json`에 phase 10 page_id 등록 여부 확인 후 진입.
