# 2026-05-08

## 한 일
- `/harness mvp` 실행, docs 3종(PRD/ARCHITECTURE/ADR) + ARCHITECTURE_v2_detailed 정독
- MVP를 ARCHITECTURE §7 누적 빌드 로드맵(0.1→1.0)에 맞춰 11 phase로 분해, 사용자와 합의
- `phases/mvp/` 스캐폴드 구축
  - `README.md` — phase별 표준 7단계 절차 명세 (계획 → adversarial review → 결과 보존 → 이슈 분류 → 구현 → 수동 검증 → 완료)
  - `phase01-bootstrap.md` ~ `phase11-stage10-bomber-polish.md` (11개)
  - `plans/`, `reviews/` 빈 폴더
  - `status.json` 자동 초기화 (11 phase pending)
- `worklog/` 폴더 신설 + README 작성, 본 파일이 첫 기록
- `scripts/execute.py` UTF-8 stdout 패치 (Windows cp949에서 em-dash UnicodeEncodeError 차단)

## 결정 / 변경
- **adversarial review 절차 강화** — 단순 실행이 아니라 (1) `reviews/phaseNN-review.md`에 stdout 그대로 보존, (2) CRITICAL/HIGH는 반드시 수정, (3) 미수정 항목은 `phaseNN-deferred.md`에 사유/재검토 시점 기록. 사유: 리뷰 결과를 휘발시키지 않고 phase 단위 의사결정 발자국을 남기기 위함.
- **Phase 분해 = 빌드 0.1~1.0 1대1 매핑** + 별도 phase 1로 부트스트랩 분리. 사유: 각 빌드는 단독 검증 가능하고 회귀 패키지가 명확. 부트스트랩을 Stage 1과 합치면 verification 스코프가 흐려짐.
- **에셋 정책 = 프로그래매틱 placeholder**. ColorRect/단색 PNG로 진행, 사운드는 PRD 따라 후반 폴리싱 (Phase 11에서도 보류).
- **Phase 1 in_progress → pending 롤백**. 사유: `python scripts/execute.py mvp next`가 started_at을 찍었는데 구현 없이 종료하면 내일 complete 시 duration이 부풀려짐.

## 산출물
- `phases/mvp/README.md`
- `phases/mvp/phase01-bootstrap.md` ~ `phase11-stage10-bomber-polish.md`
- `phases/mvp/status.json` (Phase 1~11 pending)
- `phases/mvp/plans/`, `phases/mvp/reviews/` (빈 폴더)
- `worklog/README.md`
- `worklog/2026-05/2026-05-08.md` (본 파일)
- `scripts/execute.py` (UTF-8 stdout 패치)

## 다음 진입점
**Phase 1: bootstrap부터 표준 절차 1단계로 시작.**

```bash
# 1) 상태 확인
python scripts/execute.py mvp

# 2) Phase 1 in_progress 마킹 + 정의 출력
python scripts/execute.py mvp next

# 3) phases/mvp/plans/phase01-plan.md 작성
#    (변경 파일 + 씬 트리 + 시그널 + 엣지케이스 + 검증 시나리오)

# 4) adversarial review
node "C:\Users\code1412\.claude\plugins\cache\openai-codex\codex\1.0.3\scripts\codex-companion.mjs" \
  adversarial-review --wait "phase 1 plan: project bootstrap + autoload skeleton"

# 5) phases/mvp/reviews/phase01-review.md 에 stdout 보존
# 6) CRITICAL/HIGH 모두 반영, 미수정은 phase01-deferred.md
# 7) 구현 → Godot 에디터 검증 → python scripts/execute.py mvp complete 1
```

Phase 1 변경 대상 핵심:
- `project.godot` (Godot 4.6, 1920x1080)
- `scripts/core/{GameManager,EventBus,SkillRegistry}.gd` (Autoload 빈 셸)
- `scenes/Main.tscn`
- 폴더 스켈레톤 + `.gitignore`

## 미해결
- 없음. phases/mvp/README.md의 절차가 단일 SoT, 다음 세션은 그것만 따르면 됨.
