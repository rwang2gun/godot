# TDD Guard 스텁. 진짜 coverage는 tests/CampaignUnlockOrderTest.tscn
# (first 항상 unlocked + prev cleared 시 next unlocked[manifest 순서] + 챕터 게이팅 +
#  빈 챕터 skip + 재배치 시 언락 재계산). campaign-50 Phase A.
extends Node
