# TDD Guard 스텁. 진짜 coverage는 tests/CampaignManifestTest.tscn
# (is_valid 정상/중복 씬 id/빈 챕터 거부 + ordered_stage_ids 순서 + chapter_of/next_stage_id/
#  position_of/first/last 경계). campaign-50 Phase A.
extends Node
