# TDD Guard 스텁. 진짜 coverage는 다음에서 담당:
#   - tests/SfxReceiverTest.tscn (Phase 21) — LOCKED 슬롯 emit이 clean 'locked' id인지,
#     'sfx:locked' prefix가 제거됐는지 정규화 회귀 검사 (검사 e).
# 본 stub은 StageSelect.gd 수정 시 TDD guard 통과용.
extends Node
